/**
 * Tokenization barrel export.
 */

export { getAccurateTokenCount } from './estimators'

/**
 * Processes streaming block logs for token estimation.
 */
export function processStreamingBlockLogs(_blockLogs: unknown, _streamedContent?: unknown): unknown {
  return {}
}
