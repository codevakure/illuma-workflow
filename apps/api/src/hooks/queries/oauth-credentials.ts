/**
 * OAuth credentials query stub for the API server.
 */

/**
 * Fetches OAuth credential details by credential ID and workflow ID.
 */
export async function fetchOAuthCredentialDetail(
  _credentialId: string,
  _workflowId: string
): Promise<Array<{ name?: string }>> {
  return []
}
